Bootstrap: docker
From: bioconductor/bioconductor:RELEASE_3_21

####################################################################################
# Set base environment variables
####################################################################################
%environment
    export LANG="C.UTF-8"
    export LC_ALL="C.UTF-8"
    export TZ="America/Denver"

    # Set Conda and R environment variables
    export CONDA_DIR="/opt/conda"
    export PATH="$CONDA_DIR/bin:$PATH"
    export CONDA_PREFIX="$CONDA_DIR"
    export RETICULATE_PYTHON="$CONDA_DIR/bin/python"
    export R_LIBS_USER="/home/r_libs"
    export TORCHINDUCTOR_CACHE_DIR="/tmp/torch_cache"
    export NUMBA_CACHE_DIR="/tmp/numba_cache"

    # Configure ccache
    export PATH="/usr/lib/ccache:$PATH"
    export CC="ccache gcc"
    export CXX="ccache g++"
    export CCACHE_DIR="/cache/ccache"
    
####################################################################################
# Copy the R installation script into the container before the build
####################################################################################
%files
    install_packages.R /install_packages.R
    
####################################################################################
# Execute all installation steps in a single %post section
####################################################################################
%post
    echo "Starting build process..."
    
    # Update package lists and install core dependencies
    apt-get update
    apt-get install -y --no-install-recommends \
        wget \
        gnupg \
        git \
        ccache

    # Fix GPG key import for Kitware
    wget -qO - https://apt.kitware.com/keys/kitware-archive-latest.asc | gpg --dearmor > /etc/apt/trusted.gpg.d/kitware.gpg
    echo 'deb https://apt.kitware.com/ubuntu/ jammy main' | tee /etc/apt/sources.list.d/kitware.list >/dev/null

    # Install CMake and clean up
    apt-get update
    apt-get install -y --no-install-recommends cmake
    cmake --version
    apt-get clean
    rm -rf /var/lib/apt/lists/*
    
    # Install Miniforge3 and Python packages
    wget --quiet https://github.com/conda-forge/miniforge/releases/latest/download/Miniforge3-Linux-x86_64.sh -O /tmp/miniforge.sh
    /bin/bash /tmp/miniforge.sh -b -p /opt/conda
    rm /tmp/miniforge.sh
    
    /opt/conda/bin/conda install -y -c conda-forge numpy 'scikit-learn>=1.4' pandas pytorch matplotlib aif360 tabulate jaxtyping beartype pytables opentsne umap-learn geoparse seaborn
    /opt/conda/bin/conda install -y -c conda-forge psutil tqdm rich memory_profiler py-cpuinfo
    /opt/conda/bin/pip install gdown osfclient
    
    # Clean up Conda caches
    /opt/conda/bin/conda clean --all -y
    
    # Install R packages
    mkdir -p /home/r_libs
    chmod -R 777 /home/r_libs
    Rscript /install_packages.R
    
    # Final cleanup and repository cloning
    apt-get clean
    rm -rf /var/lib/apt/lists/*
    
    mkdir -p /tmp/numba_cache
    chmod -R 777 /tmp/numba_cache
    
    git clone https://github.com/datapplab/AutoClass.git /opt/AutoClass
    
    echo "Build completed."